# Supabase connection (build-time variables — inlined by Vite at build).
# On Cloudflare Workers Builds, set these as *Build* variables, not runtime secrets.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
